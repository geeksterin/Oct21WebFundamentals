// Arrow Functions

// function name_of_function () {

//}

/**
 * const functionName = (param1, param2) => {
 *  // Write your logic here.
 * }
 * 
 * let value = functionName()
 */

// Prompt and confirm
// prompt('title', [dafault])
// this default is optional!
// it returns whatever is entered inside the input
// 

// Confirm: displays two buttons yes or no

// Comparisons
// >, <, >=, <=, ==, !=
// All these operators return a boolean expression.
// let x = 5;
/**
 * let y = prompt('enter any value')
 *  if (x == y) {
 *  alert(x)
 * }
 * 
 * '==' converts values to numbers
 * 
 * '=' assignment operator
 * let x = 6;
 * Number, Number
 * 
 * x == 6 // return true // return false
 * 
 * let x = '6'
 * 
 * String
 * 
 * x == 6 // return true
 * 
 * '==' converts every other data type into numeric value
 *  6 == 6 return true
 * 
 * when there are alphabets 
 * 
 * 'Bool' < "Boil" return false
 * B with B
 * O with O
 * O with I
 * 
 * 
 * === will check the type of the operand as well
 */


/**
 * Array collection of same type of data.
 * let car = ['sonet', 'audi', 'bmw']
 * let arr = [1,2,3]
 * let emptyArray = [];
 * 
 * push(), pop(), shift(), sort()
 * // push returns the number inserted in the array
 * // shift function removes the 0th element
 * // shift returns the value which is shifted
 */

// Null and undefined
// undefined is when a variable is declared but it does not have a value.

// NULL : it is value which CAN BE assigned to a variable and it represents that a variable does not 
// have a value.
//
// Conditional statements
// Usecase: if the customers age is below 18 
// if (condition) { // code }

/**
 * let age;
 * 
 * if (age >= 18) {
 * alert("enter the bar!")
 * } else {
 *  aleert("No entry")
 * }
 * 
 * 18-25 A
 * 25 B
 * 18 C
 * 
 * if (age >25) {
 *  alert("enter B")
 * }
 * else if (age >=18 || age <= 25) {
 *  alert("Enter A")
 * } else {
 *  alert("Enter C")
 * }
 */ 

/**
 * ? :
 * 
 * let result = condition ? value1 : value2;
 * 
 * let resultAge = age>18 ? "Entry allowed" : "No entry"
 * 
 * Logical operators && and || 
 */

// Objects:
/**
 * name, dob, college, city, age
 * 
 * let students = {
 *  name : "Isha",
 *  dob: "23.4.1994",
 *  college: "xyz",
 *  age: 26
 * }
 * 
 * array of numbers, strings, booleans, objects
 * 
 */